import { createMuiTheme } from "@material-ui/core/styles";
import { pink, red, lightBlue, blue } from "@material-ui/core/colors";

const theme = createMuiTheme({
  palette: {
    primary: pink,
    secondary: blue,
  },
});

export default theme;
